// Hero booking button
document.querySelector('.hero-book-btn')?.addEventListener('click', () => {
  alert('Redirecting you to the booking page!');
  // Example: window.location.href = "book-table.html";
});

// Subscribe button
document.querySelector('.subscribe-btn')?.addEventListener('click', () => {
  alert('Subscribed to the newsletter!');
});
